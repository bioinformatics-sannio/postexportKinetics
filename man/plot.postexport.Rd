% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.postexport}
\alias{plot.postexport}
\alias{plot.postexport_fit}
\alias{plot.postexport_test}
\alias{plot.postexport_fit_set}
\alias{plot.postexport_test_set}
\alias{plot.postexport_simulation}
\alias{plot.postexport_domain_check}
\title{Plot post-export kinetic results}
\usage{
\method{plot}{postexport_fit}(x, type = "fit", show_means = TRUE, ...)

\method{plot}{postexport_test}(x, type = c("fit", "bootstrap"), show_means = TRUE, ...)

\method{plot}{postexport_fit_set}(x, type = c("status", "boundary", "sigma_IR"), ...)

\method{plot}{postexport_test_set}(
  x,
  type = c("status", "boundary", "sigma_IR", "sigma_q", "IR_q", "score"),
  ...
)

\method{plot}{postexport_simulation}(x, show_observed = TRUE, ...)

\method{plot}{postexport_domain_check}(x, ...)
}
\arguments{
\item{x}{A result object.}

\item{type}{Plot type (see Details).}

\item{show_means}{Logical; show time-point means (fits and tests).}

\item{...}{Ignored.}

\item{show_observed}{Logical; show noisy observations (simulations).}
}
\value{
A \code{ggplot} object.
}
\description{
\code{ggplot2} plots of fit, test, simulation and operational-domain results.
Plotting never modifies the object, never refits and never runs inference;
each method returns a \code{ggplot} object.
}
\section{Fits and tests}{

\code{type = "fit"} (default) shows, for the four states \code{N}, \code{N_s}, \code{C} and
\code{C_s}, the replicate observations, the time-point means (\code{show_means}),
the sampling times, \code{t_star} and the fitted full (\code{sigma_c >= 0}) and null
(\code{sigma_c = 0}) models. The fitted curves are display reconstructions
only: they are propagated with the ODE model from the observed
replicate-mean state at the first sampled time using the fitted
coefficients and the model's transcription schedule. The fit, the test
statistic and the bootstrap/null calculations do not use these curves;
they use the validated frozen interval-balance / Crank-Nicolson machinery.
Plotting never refits or modifies the result. For
\code{postexport_test}, \code{type = "bootstrap"} shows the bootstrap distribution
of \code{T} with the observed \code{T}, the boundary atom, the p-value and the
failure fraction.
}

\section{Sets}{

\code{type = "status"} (default): counts by status; \code{"boundary"}: counts by
boundary status; \code{"sigma_IR"}: \code{sigma_c} against \code{IR}. For test sets
after \code{\link[=adjust_postexport_pvalues]{adjust_postexport_pvalues()}} also \code{"sigma_q"} (\code{sigma_c} against
\code{-log10(q)}), \code{"IR_q"} (\code{IR} against \code{-log10(q)}) and \code{"score"}
(distribution of the exploratory score of \code{\link[=rank_postexport_candidates]{rank_postexport_candidates()}}).
The axes keep effect size, fit improvement and evidence separate.
}

\section{Simulations}{

Latent trajectories per replicate (no smoothing), sampled latent states,
noisy observations (\code{show_observed}), the transcriptional onset and the
intervention time \code{t_star}.
}

\section{Operational domain}{

Manuscript benchmark designs matched exactly or as nearest evaluated
designs, with empirical Type-I error at alpha = 0.05, Wilson 95\%
intervals and the nominal 0.05 reference. This describes benchmark
simulations, not the calibration of a dataset.
}

\examples{
p <- c(R = 100, tau = 0.03, tau_s = 0.015, sigma_c = 0.1, sigma_n = 0.1,
       alpha = 0.2, alpha_s = 0.08)
sim <- simulate_postexport_kinetics(
    p, times = c(90, 100, 110, 130, 160), onset_time = 0, t_star = 100,
    regime = "SHUTOFF", time_unit = "min", n_replicates = 3,
    param_cv = 0.05, noise = list(platform = "gaussian", level = "low"),
    seed = 1)
plot(sim)
x <- postexport_data(sim$observed, time_unit = "min")
plot(fit_postexport_model(x, t_star = 100))
}
