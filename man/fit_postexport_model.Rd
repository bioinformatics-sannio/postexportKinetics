% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{fit_postexport_model}
\alias{fit_postexport_model}
\title{Fit the full and null post-export kinetic models}
\usage{
fit_postexport_model(
  data,
  t_star,
  control = postexport_control(),
  events = NULL
)
}
\arguments{
\item{data}{A \code{postexport_data} object.}

\item{t_star}{Intervention (transcriptional shutoff) time on the time axis
of \code{data}, or \code{NULL} for continuous transcription. Must be supplied
explicitly.}

\item{control}{A \code{\link[=postexport_control]{postexport_control()}} object. Only the covariance and
fitting settings are used.}

\item{events}{Optional character vector of events to fit; default all.}
}
\value{
For one event, an object of class \code{postexport_fit} with elements
\describe{
\item{\code{event}, \code{status}, \code{error_message}}{Frozen status codes: \code{"ok"},
\code{"observed_system_failed"} or \code{"observed_fit_failed"}.}
\item{\code{estimates}}{\code{sigma_c} (effect-size estimate), \code{coef_full},
\code{coef_null} (named, original scale; \code{sigma_c = 0} in the null).}
\item{\code{fit}}{\code{RSS_null}, \code{RSS_full} (whitened), \code{T_obs},
\code{delta_RSS}, \code{IR} (relative RSS improvement \code{delta_RSS / RSS_null}).}
\item{\code{boundary}}{\code{tolerance}, \code{at_boundary} (frozen rule),
\code{sigma_c_zero} (\verb{|sigma_c| < 1e-12}).}
\item{\code{diagnostics}}{condition number and minimum singular value of the
whitened full design, ranks of the full and null designs, pooled
covariance, structurally zero design columns and the condition
number over the remaining columns (diagnostic only).}
\item{\code{design}}{\code{t_star}, \code{time_unit}, intervention, time points and
replicates per time point.}
\item{\code{system}}{the interval-balance system (\code{A}, \code{b}, \code{Sigma_b},
...).}
\item{\code{control}, \code{provenance}}{settings and frozen reference.}
}
For several events, a \code{postexport_fit_set}: a list with \code{summary} (one
row per event) and \code{results} (named list of \code{postexport_fit}).
}
\description{
Fits, for each event, the full model (post-export conversion rate
\code{sigma_c >= 0}) and the constrained null model (\code{sigma_c = 0}) to
compartment-resolved time-course data, without bootstrap inference.
}
\section{Model}{

Four kinetic states are required: nuclear unprocessed \code{N}, nuclear
processed \code{N_s}, cytoplasmic unprocessed \code{C} and cytoplasmic processed
\code{C_s}, with
\deqn{dN/dt = R - (\sigma_n + \tau) N,\quad
      dN_s/dt = \sigma_n N - \tau_s N_s,}
\deqn{dC/dt = \tau N - (\sigma_c + \alpha) C,\quad
      dC_s/dt = \tau_s N_s + \sigma_c C - \alpha_s C_s.}
Parameters are estimated in the order
\verb{R, tau, tau_s, sigma_c, sigma_n, alpha, alpha_s} from trapezoidal
interval balances between consecutive time points. The measurement
covariance is estimated from replicates within time points (shrinkage and
eigenvalue floor as set by \code{\link[=postexport_control]{postexport_control()}}) and propagated to the
interval balances; both models are fitted by non-negative least squares
after whitening. The transcription input is active until \code{t_star} and
zero afterwards (complete shutoff).
}

\section{Interpretation}{

\code{sigma_c} is a phenomenological post-export conversion rate from \code{C} to
\code{C_s}. A positive estimate does not identify a unique molecular mechanism
and is not, by itself, evidence of cytoplasmic splicing. The fitted values
depend on the experimental design; in particular the complete-shutoff
assumption matters for pseudo-shutoff data, which are fitted as if
transcription stopped completely at \code{t_star}.
}

\section{Boundary}{

The statistic \code{T = max(0, RSS0 - RSS1)} is computed on the whitened scale.
The fit is on the boundary when \verb{T <= 1e-10 * max(1, |RSS0|, |RSS1|)}
(frozen rule); \code{\link[=test_postexport_conversion]{test_postexport_conversion()}} then reports \code{p = 1}.
}

\examples{
set.seed(1)
tab <- expand.grid(replicate = 1:3, time = c(-15, 0, 30, 60, 120))
tab$event <- "event_1"
tab$N <- 60 * exp(-0.03 * pmax(tab$time, 0)) * exp(rnorm(nrow(tab), 0, 0.05))
tab$N_s <- 20 * exp(-0.01 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
tab$C <- 25 * exp(-0.02 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
tab$C_s <- 40 * exp(-0.005 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
x <- postexport_data(tab, time_unit = "min")
fit <- fit_postexport_model(x, t_star = 0)
fit
summary(fit)

}
\seealso{
\code{\link[=test_postexport_conversion]{test_postexport_conversion()}}, \code{\link[=postexport_data]{postexport_data()}},
\code{\link[=postexport_control]{postexport_control()}}
}
