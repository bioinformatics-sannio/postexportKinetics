% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{postexport_control}
\alias{postexport_control}
\title{Numerical and inferential settings}
\usage{
postexport_control(
  B = 1999L,
  seed = NULL,
  lambda_time = 0.5,
  lambda_diag = 0.1,
  rel_floor = 1e-08,
  max_failure_rate = 0.05,
  scaling_A = TRUE,
  truncate_nonnegative_boot = FALSE
)
}
\arguments{
\item{B}{Number of bootstrap replicates. Frozen default \code{1999}. The
bootstrap p-value is \verb{(1 + #\{T* >= T_obs\}) / (B_valid + 1)}, so \code{B}
determines its resolution and Monte-Carlo error; it does not change the
observed fit. Used by \code{\link[=test_postexport_conversion]{test_postexport_conversion()}} only.}

\item{seed}{\code{NULL} (frozen default: the random-number generator is not
reset) or an integer seed passed to \code{\link[=set.seed]{set.seed()}} immediately before the
observed fit, exactly as in the frozen implementation. For several events,
a named integer vector with one seed per event. A fixed seed reproduces
the bootstrap draws within a matched numerical environment; across
LAPACK/BLAS builds the draws can differ because the frozen bootstrap uses
\code{\link[MASS:mvrnorm]{MASS::mvrnorm()}}. Note that, as in the frozen implementation, setting a
seed changes the global random-number state.}

\item{lambda_time}{Shrinkage of each time point's within-time covariance
towards the pooled covariance, in \verb{[0, 1]}. Frozen default \code{0.5}.
Changing it changes the estimated covariance, the whitening and hence the
fit, the statistic and the p-value.}

\item{lambda_diag}{Shrinkage of the pooled within-time covariance towards
its diagonal, in \verb{[0, 1]}. Frozen default \code{0.1}. Changing it changes the
statistical behaviour as for \code{lambda_time}.}

\item{rel_floor}{Relative eigenvalue floor used to stabilise covariance
matrices (eigenvalues are raised to \code{rel_floor} times the median positive
diagonal element). Frozen default \code{1e-8}. It only matters for
near-singular covariances; changing it can change the fit in such cases.}

\item{max_failure_rate}{Largest acceptable fraction of failed bootstrap
replicates, in \verb{[0, 1]}. Frozen default \code{0.05}. If exceeded, the p-value
is \code{NA} with status \code{"bootstrap_unstable"}. It does not change p-values
that are reported.}

\item{scaling_A}{Logical; scale design-matrix columns to unit norm before
the NNLS fits. Frozen default \code{TRUE}. This is a numerical conditioning
step; coefficients are returned on the original scale.}

\item{truncate_nonnegative_boot}{Logical; set negative values of bootstrap
samples to zero. Frozen default \code{FALSE}. Changing it changes the
bootstrap null distribution and hence the p-value.}
}
\value{
An object of class \code{postexport_control} (a named list).
}
\description{
Collects the settings passed to the frozen numerical core. All defaults
are those of the frozen manuscript implementation (tag
\code{manuscript-revision-v1.0}); results obtained with the defaults are the
manuscript procedure. The intervention time \code{t_star} is a property of the
experimental design and is therefore an explicit argument of
\code{\link[=fit_postexport_model]{fit_postexport_model()}} and \code{\link[=test_postexport_conversion]{test_postexport_conversion()}}, not a control
setting.
}
\examples{
postexport_control()
postexport_control(B = 199, seed = 1)

}
\seealso{
\code{\link[=fit_postexport_model]{fit_postexport_model()}}, \code{\link[=test_postexport_conversion]{test_postexport_conversion()}}
}
