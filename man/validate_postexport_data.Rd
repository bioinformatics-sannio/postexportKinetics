% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{validate_postexport_data}
\alias{validate_postexport_data}
\title{Validate post-export kinetics data}
\usage{
validate_postexport_data(
  x,
  format = c("wide", "long"),
  columns = NULL,
  t_star = NULL
)
}
\arguments{
\item{x}{A data frame (wide or long format) or a \code{postexport_data} object.}

\item{format}{Either \code{"wide"} or \code{"long"}; ignored for \code{postexport_data}.}

\item{columns}{Optional named character vector mapping canonical column
names to column names of \code{x}; ignored for \code{postexport_data}.}

\item{t_star}{Optional intervention (transcriptional shutoff) time, on the
time axis of the data, used for design diagnostics.}
}
\value{
An object of class \code{postexport_validation}: a list with data
frames \code{errors}, \code{warnings} and \code{info} (columns \code{event}, \code{check},
\code{message}) and \code{design} (one row per event: number of samples, time
points, and minimum/maximum replicates per time point).
}
\description{
Checks compartment-resolved time-course data before model fitting and
reports findings at three levels. Validation never modifies the data.
}
\section{Errors}{

\itemize{
\item missing identifier columns (\code{event}, \code{time}, \code{replicate}) or state
columns (\code{N}, \code{N_s}, \code{C}, \code{C_s}), or malformed long-format structure;
\item missing identifier values; non-numeric or non-finite \code{time};
\item non-numeric state columns;
\item missing (\code{NA}) or non-finite state observations;
\item duplicated \code{event}/\code{time}/\code{replicate} samples;
\item an event with fewer than two distinct time points (the frozen core
requires at least two);
\item an event without any time point having at least two replicates
(the frozen core cannot estimate the within-time covariance);
\item an invalid intervention time \code{t_star} (not \code{NULL} and not a single
finite number).
}
}

\section{Warnings}{

Negative abundances. They are accepted and used unchanged.
}

\section{Diagnostic information}{

Unsorted time points; unequal replication across time points; time points
with a single replicate (the frozen core then uses the pooled covariance
for that time point); events with exactly two time points; \code{t_star} at or
before the first sample (the transcription column of the design matrix is
then identically zero, so \code{R} is not estimable and the condition number
is infinite by construction); \code{t_star} at or after the last sample; and
\code{t_star = NULL} (continuous transcription). These are reported for
information only and are not thresholds.
}

\examples{
tab <- data.frame(
    event = "e1", time = rep(c(0, 30, 60), each = 2), replicate = 1:2,
    N = c(10, 11, 8, 9, 6, 7), N_s = c(5, 6, 5, 5, 4, 5),
    C = c(3, 3, 3, 2, 2, 2), C_s = c(9, 8, 9, 9, 8, -0.1)
)
validate_postexport_data(tab, t_star = 0)

}
\seealso{
\code{\link[=postexport_data]{postexport_data()}}
}
