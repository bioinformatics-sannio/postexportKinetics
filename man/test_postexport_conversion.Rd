% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{test_postexport_conversion}
\alias{test_postexport_conversion}
\title{Test for an additional post-export conversion component}
\usage{
test_postexport_conversion(
  data,
  t_star,
  control = postexport_control(),
  events = NULL
)
}
\arguments{
\item{data}{A \code{postexport_data} object.}

\item{t_star}{Intervention (transcriptional shutoff) time on the time axis
of \code{data}, or \code{NULL} for continuous transcription. Must be supplied
explicitly.}

\item{control}{A \code{\link[=postexport_control]{postexport_control()}} object; \code{B} and \code{seed} control
the bootstrap.}

\item{events}{Optional character vector of events to fit; default all.}
}
\value{
For one event, an object of class
\code{c("postexport_test", "postexport_fit")} with all elements of a
\code{postexport_fit} (values taken from the frozen result) plus
\describe{
\item{\code{inference}}{\code{p_value} (bootstrap, one-sided), \code{B},
\code{n_bootstrap_valid}, \code{bootstrap_failure_rate}, \code{atom_zero}
(fraction of \verb{T*} at the boundary), \code{T_boot}, and descriptions of
the statistic, alternative and method.}
\item{\code{bootstrap_diagnostics}}{condition-number summaries and
rank-deficient fraction over bootstrap replicates.}
\item{\code{null_means}}{null mean trajectory used by the bootstrap.}
\item{\code{rng}}{seed, \code{RNGkind()} and, for \code{seed = NULL}, the
random-number state before the test.}
\item{\code{raw}}{the complete frozen result list.}
}
Frozen status codes: \code{"ok"}, \code{"observed_system_failed"},
\code{"observed_fit_failed"}, \code{"null_trajectory_failed"},
\code{"bootstrap_unstable"}. For several events, a \code{postexport_test_set}
with \code{summary} and \code{results}.
}
\description{
Compares, for each event, the constrained null model (\code{sigma_c = 0}) with
the full model (\code{sigma_c >= 0}); the scientific alternative is
\code{sigma_c > 0}. The observed statistic \code{T = max(0, RSS0 - RSS1)} (whitened
non-negative least squares) is calibrated by a replicate-level generative
bootstrap under the fitted null model.
}
\section{Procedure}{

The procedure is the frozen manuscript implementation (tag
\code{manuscript-revision-v1.0}), called unchanged:
\enumerate{
\item interval-balance system and whitened NNLS fits of the full and
null models (as in \code{\link[=fit_postexport_model]{fit_postexport_model()}});
\item null mean trajectory by Crank-Nicolson propagation of the fitted
null model from the observed mean at the first time point;
\item \code{B} bootstrap datasets: at each time point, as many independent
multivariate normal samples as observed replicates, around the null
means, with the estimated within-time covariance;
\item in every bootstrap dataset the means, covariances, interval
balances and propagated covariance are rebuilt and both models are
refitted, giving \verb{T*};
\item if more than \code{max_failure_rate} of the replicates fail, the
p-value is \code{NA} (status \code{"bootstrap_unstable"});
\item boundary rule: if \verb{T <= 1e-10 * max(1, |RSS0|, |RSS1|)} the
p-value is \code{1}; otherwise the add-one p-value
\verb{(1 + #\{T* >= T\}) / (B_valid + 1)}.
}
This is not a likelihood-ratio test: no likelihood is evaluated, and the
statistic is a difference of whitened residual sums of squares.
}

\section{Interpretation}{

\code{sigma_c} is a phenomenological post-export conversion rate. A small
p-value indicates kinetic evidence consistent with an additional
post-export conversion component within the model; \code{sigma_c > 0} does not
identify a unique molecular mechanism and is not, by itself, evidence of
cytoplasmic splicing. Calibration of the bootstrap is design dependent
(sampling times relative to \code{t_star}, replication, noise), and
pseudo-shutoff data are fitted as if transcription stopped completely at
\code{t_star}. The p-value is the inferential quantity; \code{sigma_c} is an
effect-size estimate; neither is a ranking score. No multiple-testing
adjustment is applied: for several events, adjust the p-values
explicitly, e.g. with \code{\link[stats:p.adjust]{stats::p.adjust()}}.
}

\section{Reproducibility}{

With a fixed \code{seed}, the bootstrap draws are reproducible within a matched
numerical environment (operating system, LAPACK/BLAS, R and package
versions). Across LAPACK/BLAS builds the draws, and hence Monte-Carlo
p-values, can differ even under the same seed, because the frozen
bootstrap uses \code{\link[MASS:mvrnorm]{MASS::mvrnorm()}}. As in the frozen implementation, a
non-\code{NULL} seed resets the global random-number state.
}

\examples{
set.seed(1)
tab <- expand.grid(replicate = 1:3, time = c(0, 15, 30, 60, 120))
tab$event <- "event_1"
tab$N <- 60 * exp(-0.03 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
tab$N_s <- 20 * exp(-0.01 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
tab$C <- 25 * exp(-0.02 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
tab$C_s <- 40 * exp(-0.005 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
x <- postexport_data(tab, time_unit = "min")
res <- test_postexport_conversion(
    x, t_star = 0, control = postexport_control(B = 19, seed = 1)
)
res

}
\seealso{
\code{\link[=fit_postexport_model]{fit_postexport_model()}}, \code{\link[=postexport_control]{postexport_control()}}
}
