% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{as.data.frame.postexport}
\alias{as.data.frame.postexport}
\alias{as.data.frame.postexport_fit}
\alias{as.data.frame.postexport_fit_set}
\alias{as.data.frame.postexport_test_set}
\title{Tidy result tables}
\usage{
\method{as.data.frame}{postexport_fit}(x, row.names = NULL, optional = FALSE, ...)

\method{as.data.frame}{postexport_fit_set}(x, row.names = NULL, optional = FALSE, ...)

\method{as.data.frame}{postexport_test_set}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{A \code{postexport_fit}, \code{postexport_test}, \code{postexport_fit_set} or
\code{postexport_test_set}.}

\item{row.names, optional}{Ignored (for compatibility with the generic).}

\item{...}{Ignored.}
}
\value{
A data frame with one row per event.
}
\description{
Flat, stable, user-facing tables of fit and test results, suitable for
saving (for example with \code{\link[utils:write.csv]{utils::write.csv()}}). Internal matrices are not
included; the per-event result objects remain available unchanged.
}
\section{Columns}{

\describe{
\item{identifier / status}{\code{event}, \code{status}, \code{error_message}.}
\item{inference}{\code{p_value} (bootstrap, one-sided), \code{q_value} (only
after \code{\link[=adjust_postexport_pvalues]{adjust_postexport_pvalues()}}; otherwise absent),
\code{adjustment_group}, \code{B}, \code{n_bootstrap_valid},
\code{bootstrap_failure_rate}, \code{atom_zero}.}
\item{effect size}{\code{sigma_c} (phenomenological post-export conversion
rate).}
\item{fit}{\code{IR} (relative RSS improvement), \code{T_obs}, \code{RSS_null},
\code{RSS_full}.}
\item{boundary}{\code{at_boundary} (frozen rule), \code{sigma_c_zero},
\code{boundary_tolerance}.}
\item{diagnostics}{\code{condition_number},
\code{condition_number_nonzero_columns}, \code{min_singular_value},
\code{rank_full}, \code{rank_null}, \code{structurally_zero_columns}.}
\item{design}{\code{t_star}, \code{time_unit}, \code{n_time_points},
\code{min_replicates}, \code{design_warnings}.}
}
The role of every column is also given in the attribute
\code{"column_roles"}.
}

\examples{
set.seed(1)
tab <- expand.grid(replicate = 1:3, time = c(-15, 0, 30, 60, 120))
tab$event <- "event_1"
tab$N <- 60 * exp(-0.03 * pmax(tab$time, 0)) *
    exp(rnorm(nrow(tab), 0, 0.05))
tab$N_s <- 20 * exp(-0.01 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
tab$C <- 25 * exp(-0.02 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
tab$C_s <- 40 * exp(-0.005 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
fit <- fit_postexport_model(postexport_data(tab, time_unit = "min"),
                            t_star = 0)
as.data.frame(fit)
}
