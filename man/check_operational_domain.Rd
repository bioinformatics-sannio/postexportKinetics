% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/domain.R
\name{check_operational_domain}
\alias{check_operational_domain}
\title{Compare a design with the manuscript benchmark (diagnostic only)}
\usage{
check_operational_domain(
  data = NULL,
  regime,
  t_star = NULL,
  platform = NULL,
  noise_level = NULL,
  n_time_points = NULL,
  n_replicates = NULL,
  sampling_interval = NULL,
  time_unit = NULL
)
}
\arguments{
\item{data}{Optional \code{postexport_data} object. The number of time points,
the minimum number of replicates per time point, the sampling interval
(if regular) and the time unit are then taken from each event. Do not
combine with \code{n_time_points}, \code{n_replicates}, \code{sampling_interval} or
\code{time_unit}.}

\item{regime}{\code{"NONE"}, \code{"SHUTOFF"} or \code{"PSEUDO_SHUTOFF"}; required. Not
inferred from the data.}

\item{t_star}{Intervention time (for \code{data} with a shutoff regime); used to
describe the sampling layout around \code{t_star}.}

\item{platform}{Optional measurement platform (\code{"gaussian"}, \code{"rnaseq"},
\code{"rtqpcr"}); never inferred from abundances. \code{NULL} reports all
platforms.}

\item{noise_level}{Optional noise level (\code{"very_low"}, \code{"low"},
\code{"medium"}, \code{"high"}); never inferred. \code{NULL} reports all levels.}

\item{n_time_points, n_replicates, sampling_interval}{Explicit design
(without \code{data}): number of distinct sampling times, replicates per time
point and regular sampling interval (\code{NA} if irregular).}

\item{time_unit}{Time unit of \code{sampling_interval} (without \code{data}).}
}
\value{
An object of class \code{postexport_domain_check}: a list with
\code{designs} (one row per distinct design and the events sharing it),
\code{checks} (per design: \code{design}, \code{match_type} = \code{"exact"}, \code{"nearest"} or
\code{"not_benchmarked"}, \code{matches} (benchmark configurations), \code{differences}
(for nearest designs: user value and benchmark levels per dimension),
\code{summary}, \code{statements} and \code{caveats}) and \code{provenance}.
}
\description{
Reports how configurations of the manuscript's corrected factorial
benchmark that match (or are nearest to) an experimental design behaved:
empirical Type-I error at alpha = 0.05 with its Wilson 95\% interval,
whether the manuscript's operational criterion was met for those benchmark
configurations, and power. This is an empirical comparison with simulated
benchmark designs; it is not a calibration guarantee for any dataset, it
never blocks or alters inference and it gives no valid/invalid verdict.
}
\section{Benchmark}{

1152 configurations of the corrected factorial benchmark (tag
\code{manuscript-revision-v1.0}): regimes \code{NONE} (continuous transcription) and
\code{SHUTOFF} (complete shutoff); platforms \code{gaussian}, \code{rnaseq}, \code{rtqpcr};
noise levels \code{very_low}, \code{low}, \code{medium}, \code{high}; 3, 5 or 10 replicates;
sampling intervals 5, 10, 20, 50; nominal 3, 5, 10 or 20 time points
(matching uses the effective number of distinct sampling times). Each
configuration simulated 2000 genes (about 20\% with \code{sigma_c > 0}) with
rates per minute, a common shutoff time 332, onset shifts between -100 and
100
and B = 1999 bootstrap replicates. SHUTOFF designs sampled one point at
\code{t_star - interval}, one at \code{t_star} and the remaining points at spacing
\code{interval} after \code{t_star}. Pseudo-shutoff designs are not part of this
benchmark. The operational criterion of the manuscript
(\code{analyze_benchmark_corrected_onset_final.R}) is that the Wilson 95\%
interval of the empirical Type-I error contains 0.05; the Type-I error
lying in [0.025, 0.075] is reported as a secondary descriptive flag.
}

\section{Matching}{

A benchmark configuration matches exactly when the regime, the platform
and noise level (when supplied; otherwise all are included), the number of
time points, the number of replicates and, for \code{SHUTOFF}, the sampling
interval are equal. The sampling interval is compared only when it is
regular and the time unit is minutes (the benchmark time scale). Without
an exact match, the nearest evaluated designs are reported, selected
lexicographically (nearest sampling interval, then nearest number of time
points, then nearest number of replicates, among configurations with the
same regime, platform and noise level); they are labelled as nearest
benchmark designs, not equivalent designs. No calibration is interpolated
and no score is computed.
}

\examples{
check_operational_domain(regime = "SHUTOFF", platform = "rnaseq",
                         noise_level = "medium", n_time_points = 5,
                         n_replicates = 3, sampling_interval = 10,
                         time_unit = "min")
check_operational_domain(regime = "NONE", n_time_points = 5,
                         n_replicates = 3)

}
\seealso{
\code{\link[=test_postexport_conversion]{test_postexport_conversion()}}
}
