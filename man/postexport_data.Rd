% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{postexport_data}
\alias{postexport_data}
\title{Create a validated post-export kinetics data object}
\usage{
postexport_data(x, time_unit, format = c("wide", "long"), columns = NULL)
}
\arguments{
\item{x}{A data frame in wide or long format.}

\item{time_unit}{Character string naming the unit of \code{time} (for example
\code{"min"}). Required: kinetic rates, including \code{sigma_c}, are expressed
per this unit.}

\item{format}{Either \code{"wide"} (default, canonical) or \code{"long"}.}

\item{columns}{Optional named character vector mapping canonical column
names to column names of \code{x}.}
}
\value{
An object of class \code{postexport_data}: a data frame with columns
\code{event} (character), \code{time} (numeric), \code{replicate} (character), \code{N},
\code{N_s}, \code{C}, \code{C_s} (numeric), and attributes \code{time_unit}, \code{format} (the
input format) and \code{validation} (a \code{postexport_validation} object).
}
\description{
Builds the canonical representation of compartment-resolved time-course
measurements for the four kinetic states used by the model:
nuclear unprocessed (\code{N}), nuclear processed (\code{N_s}), cytoplasmic
unprocessed (\code{C}) and cytoplasmic processed (\code{C_s}) RNA.
}
\section{Input formats}{

\describe{
\item{\code{format = "wide"} (canonical)}{One row per destructive biological
sample, with columns \code{event}, \code{time}, \code{replicate}, \code{N}, \code{N_s}, \code{C},
\code{C_s}.}
\item{\code{format = "long"}}{One row per sample and state, with columns
\code{event}, \code{time}, \code{replicate}, \code{compartment}, \code{state}, \code{abundance}.
\code{compartment} must be \code{"nuclear"} or \code{"cytoplasmic"} and \code{state} must
be \code{"unprocessed"} or \code{"processed"}. The mapping is fixed:
nuclear/unprocessed = \code{N}, nuclear/processed = \code{N_s},
cytoplasmic/unprocessed = \code{C}, cytoplasmic/processed = \code{C_s}. For
retained-intron events quantified by rMATS, inclusion corresponds to
\code{unprocessed} and skipping to \code{processed}; that assignment is the
user's responsibility and is not inferred. Every sample must have
exactly one value for each of the four states.}
}
Column names are matched exactly. Different names can be declared with
\code{columns}, a named character vector mapping canonical names to the names
in \code{x} (for example \code{c(event = "event_id", time = "minutes")}). No other
column is interpreted.
}

\section{Design semantics}{

Each row is a destructive sample: replicate labels identify samples within
a time point and are never used to pair samples across time points.
Unequal numbers of replicates per time point are allowed. Row order is
preserved.
}

\section{Missing values}{

Missing states are an error. The constructor never converts missing values
or missing states to zero and never imputes them. Samples with incomplete
measurements must be removed (or completed) explicitly before
construction.
}

\examples{
set.seed(1)
tab <- expand.grid(replicate = 1:3, time = c(0, 15, 30, 60, 120))
tab$event <- "event_1"
tab$N <- 60 * exp(-0.03 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
tab$N_s <- 20 * exp(-0.01 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
tab$C <- 25 * exp(-0.02 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
tab$C_s <- 40 * exp(-0.005 * tab$time) * exp(rnorm(nrow(tab), 0, 0.05))
x <- postexport_data(tab, time_unit = "min")
x

}
\seealso{
\code{\link[=validate_postexport_data]{validate_postexport_data()}}, \code{\link[=fit_postexport_model]{fit_postexport_model()}},
\code{\link[=test_postexport_conversion]{test_postexport_conversion()}}
}
